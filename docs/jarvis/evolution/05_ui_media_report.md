# UI and Media Report

`MediaPlayer` now builds an AudioContext/AnalyserNode only after user-initiated playback, renders real spectrum data, stops animation on pause, and closes resources on unmount. Existing layout is retained. Full visual/browser proof and a YouTube mini-player remain NOT_AVAILABLE in this pass.
